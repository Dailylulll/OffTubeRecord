{
  "name": "offtube-record-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "npm run start --prefix server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
